import { FileText } from 'lucide-react';

/**
 * DocumentIcon
 * 
 * Icon component for the Document Management module.
 * Uses the FileText icon from lucide-react.
 */
const DocumentIcon = () => {
  return <FileText className="h-6 w-6" />;
};

export default DocumentIcon;

