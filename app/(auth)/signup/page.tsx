import SignupForm from '@/components/auth/SignupForm';

export const metadata = {
  title: 'Sign Up - Safety Management',
  description: 'Create a new safety management account',
};

export default function SignupPage() {
  return <SignupForm />;
}

